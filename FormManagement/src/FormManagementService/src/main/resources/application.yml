server:
  port: 3001

spring:
  banner:
    location: banner.txt
  datasource:
    url: jdbc:postgresql://localhost:5432/quizgpt
    username: admin
    password: password

#production postgresql
#  datasource:
#    url: jdbc:postgresql://${postgresql.host}:${postgresql.port}/quizgpt
#    username: ${postgresql.username}
#    password: ${postgresql.password}

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
#
#  rabbitmq:
#    host: ${rabbitmq.host}
#    port: ${rabbitmq.port}
#    username: ${rabbitmq.username}
#    password: ${rabbitmq.password}
  main:
    web-application-type: servlet

